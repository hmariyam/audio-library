<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AudioLibraryTP2"
        tools:targetApi="31">
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--Pour passer à la pageAccueil-->
        <activity
            android:name=".ui.main.PageAccueil"
            android:exported="true">
        </activity>

        <!--Pour passer à la pageFormulaire-->
        <activity
            android:name=".ui.main.PageFormulaire"
            android:exported="true">
        </activity>

        <!--Pour passer à la pageLibrarie-->
        <activity
            android:name=".ui.main.PageLibrarie"
            android:exported="true">
        </activity>

        <!--Pour passer à la pageModification-->
        <activity
            android:name=".ui.main.PageModification"
            android:exported="true">
        </activity>
    </application>

</manifest>